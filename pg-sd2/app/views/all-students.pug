extends layout

block content
    - console.log(data)
    .main-content 
        h1 All Students
        table(border=1)
            each item in data 
                tr  
                    td #{item.id}
                    td 
                        a(href='/student-single/' + item.id) #{item.name}
